@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<style>
    #AddMenuForm {
        align-self: center;
        margin-top: 10px;
    }

    #Addmenu {
        position: absolute;
        top: 63px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        border: 1px solid black;
        background-color: #1D252A;
    }

        #Addmenu button {
            font-size: 16px;
            color: white;
            margin: 5px;
            margin-top: 1px;
            margin-bottom: 1px;
            border: none;
            background-color: transparent;
        }

        #Addmenu[aria-hidden="true"] {
            display: none;
        }

    #AddImageContainer {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }

    #pen {
        width: 50px;
        height: 50px;
    }
</style>

<form id="AddMenuForm" method="post" action="@Url.Action("SelectionFromUser", "Home")">
    <div id="AddImageContainer">
        <img id="pen" scr="/img/Pen.jpg" aria-expanded="false" />

        <div id="Addmenu" aria-hidden="true">
            <button name="selectedValue" action="@Url.Action("SelectionFromUser", "Home")" value="animeList">Список Аниме</button>
            <button name="selectedValue" action="@Url.Action("SelectionFromUser", "Home")" value="mangaList">Список Манги</button>
            <button name="selectedValue" action="@Url.Action("SelectionFromUser", "Home")" value="ranobeList">Список Ранобэ</button>
            <button name="selectedValue" action="@Url.Action("SelectionFromUser", "Home")" value="settings">Настройки</button>
            <button name="selectedValue" action="@Url.Action("SelectionFromUser", "Home")" value="logout">Выйти</button>
        </div>
    </div>
</form>

<script>
    const menuToggle = document.querySelector('#pen');
    const menu = document.querySelector('#Addmenu');

    menuToggle.addEventListener('click', function () {
        const expanded = menuToggle.getAttribute('aria-expanded') === 'true' || false;
        menuToggle.setAttribute('aria-expanded', !expanded);
        menu.setAttribute('aria-hidden', expanded);
    });
</script>
